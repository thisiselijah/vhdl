library ieee;
use ieee.std_logic_1164.all;

entity flappyBirds is 
	port(clk, reset, start : in std_logic;
		 led_matrix        : out std_logic_vector(15 downto 0);
		 segments          : out std_logic_vector(6 downto 0));
end flappyBirds;

architecture behavioral of flappyBirds is
signal score : INTEGER := 0 ;
signal anim_leds : std_logic_vector(15 downto 0);
signal game_leds : std_logic_vector(15 downto 0);
signal current_leds : std_logic_vector(15 downto 0);
signal display_segments : std_logic_vector(6 downto 0;)

component animation 
	port(clk, reset : in std_logic;
		 led_maxtrix : out std_logic_vector(15 downto 0));
end component;

component game 
	port(clk, reset, start : in std_logic;
		 led_matrix        : out std_logic_vector(15 downto 0);
		 score : out INTEGER);
end component;

component seg7 
	port(score : in INTEGER;
		 segments : out std_logic_vector(6 downto 0));
end component;

begin 

u1:
u2:
u3:

end behavioral;











